#include "main.h"

/**
 * positive_or_negative - prints if integer is negative or positive
 * @i: character to check
 * Return: Always 0
 */

void positive_or_negative(int i)
{
	i = 0;
	positive_or_negative(i);

	return (0);
}
