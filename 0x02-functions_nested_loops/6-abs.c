#include "main.h"

/**
 * _abs - a function that computes the absolute value of an integer.
 * @n: parameter to be checked
 * Retun: 0
 */

 int _abs(int)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
