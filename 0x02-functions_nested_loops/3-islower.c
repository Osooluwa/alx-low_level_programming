#include "main.h"

/**
 * _islower - a function that checks for lowercase character
 * 1 if lowercase otherwise 0
 * @c - character to be checked
 * Return: Always 0 (Success)
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
